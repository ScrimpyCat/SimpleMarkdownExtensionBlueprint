# SimpleMarkdownExtensionBlueprint
An extension for SimpleMarkdown to introduce some markup to create and embed a blueprint graph.

```elixir
def deps do
    [{ :simple_markdown_extension_blueprint, "~> 0.2.0" }]
end
```
